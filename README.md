# Docker_repo

This repository contains a simple example `Dockerfile`.

## Build the image

```bash
docker build -t docker-repo-example .
```

## Run the container

```bash
docker run --rm docker-repo-example
```

The container prints a message and exits.